import { connect } from 'react-redux'
import App from '../components/App'

const mapStateToProps = (state) => {
  return {}
}

const mapDispatchToProps = (dispatch) => {
  return {}
}

const AppCtrl = connect(
  mapStateToProps,
  mapDispatchToProps
)(App)

export default AppCtrl
